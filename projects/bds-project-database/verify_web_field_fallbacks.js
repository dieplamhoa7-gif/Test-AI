const{chromium}=require('playwright');(async()=>{const b=await chromium.launch({headless:true}),p=await b.newPage({viewport:{width:1600,height:1000}});await p.goto('http://127.0.0.1:8765/index.html',{waitUntil:'networkidle'});const result=await p.evaluate(()=>{const keys=[['irr','irr_clean'],['far','far_clean'],['population','population_clean'],['revenue','revenue_clean']];for(const [legacy,clean] of keys){const r=FULL_PROJECTS.find(x=>x[legacy]&&!x[clean]);if(r){renderDetail(fullRowToProject(r));return {found:true,name:r.project_name,legacy,raw:r[legacy],detailHasFallback:document.querySelector('#detailPanel').innerText.includes(r[legacy])};}}return {found:false};});console.log(JSON.stringify(result,null,2));await b.close()})();